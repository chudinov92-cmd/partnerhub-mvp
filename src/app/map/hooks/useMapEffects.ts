"use client";

/**
 * Side-effects for map page: deeplinks, onboarding redirect, filters persistence, paywall resume.
 * Handler implementations live in useMapPageController; effects are grouped here for maintainability.
 */
export { useMapPageController as useMapEffectsSource } from "./useMapPageController";
