"use client";

/**
 * Chat handlers for map page DM/support flows.
 * Full implementation is composed in useMapPageController via useMapPageState + chat services.
 */
export type { MapPageController as ChatHandlersContext } from "./useMapPageController";
