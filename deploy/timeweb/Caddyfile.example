test.zeip.ru {
  encode gzip zstd
  reverse_proxy 127.0.0.1:3001
}

supabase.zeip.ru {
  encode gzip zstd
  reverse_proxy 127.0.0.1:8443
}

# Основной домен закрыт до возврата на prod (152-ФЗ / РКН):
zeip.ru, www.zeip.ru {
  respond "Сайт временно недоступен" 404
}
